# ═══════════════════════════════════════════════════════════════════════════
# J.A.R.V.I.S. — SALLES INDUSTRIES Quantum OS
# Dependências Python do projeto
# ═══════════════════════════════════════════════════════════════════════════

# ── Core / GUI ──
PySide6>=6.5.0
pyautogui>=0.9.54
pynput>=1.7.6

# ── Voice (TTS/STT) ──
pyttsx3>=2.90
SpeechRecognition>=3.10.0
edge-tts>=6.1.0

# ── AI / HTTP ──
httpx>=0.27.0

# ── RAG / Vector DB ──
chromadb>=0.4.0

# ── Video / Audio Processing ──
faster-whisper>=1.0.0
yt-dlp>=2024.0.0

# ── Hardware Monitoring ──
psutil>=5.9.0
gputil>=1.4.0

# ── YouTube Transcript ──
youtube-transcript-api>=0.6.0

# ── Web Automation (opcional — Playwright) ──
# playwright>=1.45.0
#   Após instalar, rode: python -m playwright install chromium

# ── Screen Context Inspector / OCR (opcional) ──
# pytesseract>=0.3.10
# Pillow>=10.0.0
#   Requer o binário Tesseract OCR instalado no sistema.

# ── Meeting Summarizer (opcional — gravação de áudio) ──
# PyAudio>=0.2.13
#   No Windows, prefira: pip install pipwin && pipwin install pyaudio

# ── Database Assistant (opcional — drivers SQL) ──
# psycopg2-binary>=2.9.0   # PostgreSQL
# pymysql>=1.1.0           # MySQL

# ── Virtual Lab / Cyber Range ──
#   Requer o Docker Desktop (CLI `docker`) e/ou WSL2 instalados no host.
#   Não há pacote pip necessário — o módulo usa subprocess.
